package io.suzuai.app.ui.theme

import androidx.compose.ui.graphics.Color

/** Hacker-terminal palette. Black background, cyan/green accents. */
object SuzuColors {
    val Background = Color(0xFF0A0F12)
    val Surface = Color(0xFF0E1418)
    val SurfaceVariant = Color(0xFF111A20)
    val Border = Color(0xFF1F2A33)

    val OnBackground = Color(0xFFC9D6DD)
    val OnSurface = Color(0xFFC9D6DD)
    val Muted = Color(0xFF6E7E88)

    val AccentCyan = Color(0xFF22D3EE)
    val AccentGreen = Color(0xFF4ADE80)
    val AccentRed = Color(0xFFEF4444)
    val AccentYellow = Color(0xFFFBBF24)

    val UserBubble = Color(0xFF11212B)
    val AssistantBubble = Color(0xFF0E1418)
    val ToolBubble = Color(0xFF0B1B24)

    val CodeBackground = Color(0xFF050A0D)
}
